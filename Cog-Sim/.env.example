# LLM 调用配置（复制本文件为 .env 并填入真实值；.env 已被 .gitignore 排除）
# DeepSeek API key（judge / 用户模拟 / 标注等所有 LLM 调用共用）
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# 可选：覆盖默认模型（默认 deepseek-flash）
MODEL=deepseek-flash
